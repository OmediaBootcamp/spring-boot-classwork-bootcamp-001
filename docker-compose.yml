version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: spring
      MONGO_INITDB_ROOT_PASSWORD: boot
    ports:
      - 27017:27017